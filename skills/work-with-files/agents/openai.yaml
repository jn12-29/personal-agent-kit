interface:
  display_name: "Work With Files"
  short_description: "Persist useful work artifacts"
  default_prompt: "Use $work-with-files to save useful working artifacts as files for review, handoff, reuse, recovery, audit, or reproducible verification. Own persistence and storage hygiene for scratch notes, findings, scripts, observations, decisions, and outputs; not planning/spec structure."
