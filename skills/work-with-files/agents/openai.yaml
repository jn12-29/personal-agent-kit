interface:
  display_name: "Work With Files"
  short_description: "Persist useful work artifacts"
  default_prompt: "Use $work-with-files to persist useful intermediate results, scripts, scratch data, findings, decisions, verification outputs, lessons, and supporting work artifacts as files when they have concrete durable value. Requirement, plan, spec, contract, and handoff state follow the repo's planning/spec convention."
