interface:
  display_name: "Work With Files"
  short_description: "Persist task state in isolated workspaces"
  default_prompt: "Use $work-with-files to preserve durable task state in an isolated workspace under .work, resume matching work, and keep its Git history separate from the host repository."
