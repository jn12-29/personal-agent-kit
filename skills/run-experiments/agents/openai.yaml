interface:
  display_name: "Run Experiments"
  short_description: "Run and monitor experiments to verified results"
  default_prompt: "Use $run-experiments to run and monitor this experiment through verified completion, including GPU-aware placement, adaptive scheduled checks, recovery when needed, durable goals for long runs, and detailed reporting."
