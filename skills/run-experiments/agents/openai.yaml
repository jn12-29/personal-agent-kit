interface:
  display_name: "Run Experiments"
  short_description: "Run and wait for experiments through verification"
  default_prompt: "Use $run-experiments to run, wait for, recover, and verify this experiment through a terminal state, using one durable Goal for clearly long runs and ETA-driven framework-native waiting within the current turn."
