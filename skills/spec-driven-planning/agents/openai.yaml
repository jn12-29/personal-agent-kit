interface:
  display_name: "Spec Driven Planning"
  short_description: "Own plans/specs and active goal state"
  default_prompt: "Use $spec-driven-planning to create the smallest useful requirement/goal snapshot, plan, spec, documentation contract, or agent handoff; resolve durable active planning state only when the task needs it."
